{
  "properties": {
    "IATA": {
      "type": "string",
      "minLength": 2,
      "maxLength": 2,
      "description": "Unique IATA code for the airlines"
    },
    "ICAO": {
      "type": "string",
      "minLength": 3,
      "maxLength": 3,
      "description": "Unique ICAO code for the airlines"
    },
    "name": {
      "type": "string",
      "minLength": 2,
      "description": "Airlines's name"
    },
    "country": {
      "type": "string",
      "minLength": 2,
      "description": "Airlines's country"
    }
  },
  "required": [
    "IATA",
    "name",
    "country"
  ],
  "additionalProperties": false
}